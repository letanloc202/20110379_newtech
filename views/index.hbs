<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>My Blog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f7f7f7;
            color: #333;
        }

        h2 {
            color: #4c79af;
            margin-top: 20px;
        }

        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        li {
            margin-bottom: 10px;
        }

        a {
            color: #333;
            text-decoration: none;
        }

        button {
            padding: 10px 20px;
            background-color: #4c79af;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 10px;
        }

        button:hover {
            background-color: #2e528e;
        }
    </style>
</head>
<body>
    <h2>Danh sách các bài viết</h2>
    <ul>
        {{#each posts}}
        <li>
            <a href="/post/{{this.id}}">{{this.title}}</a>
            <button onclick="window.location.href='/edit/{{this.id}}'">Sửa</button>
            <button onclick="deletePost('{{this.id}}')">Xóa</button>
        </li>
        {{/each}}
    </ul>

    <script>
        function deletePost(postId) {
            if (confirm('Bạn có chắc muốn xóa bài viết này không?')) {
                window.location.href = '/delete/' + postId;
            }
        }
    </script>
</body>
</html>